9/28 2020
Optimization the performance when touch the platform.
Fix the performance when touch platform and block at the same time.
IMPORTANT:If you use move blocks/platforms, please use integer numbers,use 0.x h/vspeed will cause bugs.(fuck yoyogames)
10/26 2020
Add fullscreen switch(alt + window),and make sure the application surface is correct size.
Add some scripts to protect game not crash by using unexists surface:
SurfaceCheck(surfaceID,width,height) && SurfaceCheckApplication(surfaceID)
Changed the way to draw pause screen.
Add some scripts to change/fade the view.
Fix some bugs.
Add more bugs.